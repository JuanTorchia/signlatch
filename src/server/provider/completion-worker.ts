import type {Sql}from "postgres";import type{ImmutableArtifactStore}from "@/core/pdf/preparation";
export interface ExecutedDocumentClient{downloadExecutedDocument(envelopeId:string):Promise<Uint8Array>}
export class CompletionWorker{constructor(private readonly sql:Sql,private readonly client:ExecutedDocumentClient,private readonly artifacts:ImmutableArtifactStore){}async complete(envelopeId:string){const rows=await this.sql<Array<{dispatch_id:string;status:string;lifecycle_state:string}>>`select dispatch_id,status,lifecycle_state from esign_dispatches where provider_envelope_id=${envelopeId}`;const dispatch=rows[0];if(!dispatch||dispatch.lifecycle_state!=="completed")throw new Error("Envelope is not verified complete");const bytes=await this.client.downloadExecutedDocument(envelopeId);const artifact=await this.artifacts.putPdf(bytes);await this.sql`insert into executed_documents(dispatch_id,artifact_sha256,actual_size,storage_key,provider_envelope_id) values(${dispatch.dispatch_id},${artifact.sha256},${artifact.size},${artifact.storageKey},${envelopeId}) on conflict(dispatch_id) do nothing`;return artifact;}}
