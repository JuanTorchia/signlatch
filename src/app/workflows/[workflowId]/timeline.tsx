export type TimelineEvent={event_id:string;event_type:string;occurred_at:string;provider_envelope_id:string};
export function Timeline({events}:{events:TimelineEvent[]}){return <section aria-labelledby="timeline-title"><h2 id="timeline-title">Verified provider timeline</h2>{events.length?<ol>{events.map(event=><li key={event.event_id}><time dateTime={event.occurred_at}>{new Date(event.occurred_at).toISOString()}</time> — <strong>{event.event_type}</strong> <code>{event.provider_envelope_id}</code></li>)}</ol>:<p>No authenticated provider events have been recorded.</p>}</section>}
