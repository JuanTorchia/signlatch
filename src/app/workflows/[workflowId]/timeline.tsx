export type TimelineEvent={event_id:string;event_type:string;occurred_at:string;provider_envelope_id:string};
export function Timeline({events}:{events:TimelineEvent[]}){return <section className="review-card timeline-card" aria-labelledby="timeline-title"><p className="step-label">Delivery history</p><h2 id="timeline-title">Foxit activity</h2>{events.length?<ol>{events.map(event=><li key={event.event_id}><time dateTime={event.occurred_at}>{new Date(event.occurred_at).toLocaleString()}</time><strong>{event.event_type}</strong></li>)}</ol>:<p className="muted-copy">No delivery events yet because this agreement has not been sent.</p>}</section>}
