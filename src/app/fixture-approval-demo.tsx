"use client";
import{useState}from"react";
const categories=["artifact","recipient","field","finding","intent"] as const;
export function FixtureApprovalDemo(){const[status,setStatus]=useState("review");const[category,setCategory]=useState<string>("");return <section className="demo shell" aria-labelledby="fixture-approval-title"><p className="eyebrow">Fixture-demonstrated · zero external effects</p><h2 id="fixture-approval-title">Try the exact approval latch</h2><p role="status" aria-live="polite">{status==="review"?"Exact fixture is ready for human review.":`Approval invalidated by ${category} mutation. Restoring values requires reapproval.`}</p><fieldset><legend>Material mutation matrix</legend>{categories.map(item=><button className="button button-secondary" type="button" key={item} onClick={()=>{setCategory(item);setStatus("invalidated");}}>Mutate {item}</button>)}</fieldset><label><input type="checkbox" disabled={status!=="review"}/> I reviewed the exact fixture snapshot.</label><button className="button button-primary" type="button" disabled>Approve exact fixture · read-only demonstration</button><p>No API request, provider call, signing action, or credit use can originate here.</p></section>}
