import{requireCurrentCapability}from "@/server/auth/authorize";import{sessionFromRequest}from "@/server/auth/request-session";import{database}from "@/server/database";import{TimelineStore}from "@/server/provider/timeline-store";import{SecurityStore}from "@/server/workflow/security-store";
export async function GET(request:Request,context:RouteContext<"/api/workflows/[workflowId]/timeline">){try{const session=sessionFromRequest(request);const sql=database();await requireCurrentCapability(new SecurityStore(sql),session,"read");const{workflowId}=await context.params;return Response.json({events:await new TimelineStore(sql).get(workflowId,session.tenantId)});}catch{return new Response("Not found",{status:404});}}
