import{expect,test}from"@playwright/test";
test("fresh fixture judge journey is reproducible well under ten minutes",async({page})=>{const started=Date.now();await page.goto("/");await expect(page.getByText("Sanitized fixture · zero external effects")).toBeVisible();await page.getByRole("button",{name:"Mutate intent"}).click();await expect(page.getByRole("status")).toContainText("Restoring values requires reapproval");await expect(page.getByText("No API request, provider call, signing action, or credit use can originate here.")).toBeVisible();expect(Date.now()-started).toBeLessThan(600_000);});
