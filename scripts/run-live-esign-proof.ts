const args=new Map<string,string>();for(let i=2;i<process.argv.length;i+=2){const key=process.argv[i],value=process.argv[i+1];if(key?.startsWith("--")&&value)args.set(key.slice(2),value);}const required=["workflow","review-digest","artifact-sha256","recipient","budget","authorization-id"];for(const key of required)if(!args.get(key))throw new Error(`Live proof gate missing --${key}`);if(args.get("budget")!=="1")throw new Error("Live proof budget must equal exactly one");if(process.env.SIGNLATCH_ESIGN_ENQUEUE_ENABLED!=="true"||process.env.SIGNLATCH_LIVE_PROOF_AUTHORIZATION_ID!==args.get("authorization-id"))throw new Error("Live proof is disabled without matching immediate human authorization");throw new Error("Safety stop: operator script validated the grant but live provider execution must be invoked through the authenticated dispatcher UI in this revision");
